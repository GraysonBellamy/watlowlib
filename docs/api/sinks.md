---
description: watlowlib.sinks — the SampleSink Protocol, pipe() glue, sample_to_row(), and first-party sinks (InMemory, CSV, JSONL, SQLite, Parquet, Postgres).
---

# `watlowlib.sinks`

The `SampleSink` Protocol, `pipe()` glue, `sample_to_row()` flatten,
and the first-party sinks (InMemory / CSV / JSONL / SQLite / Parquet /
Postgres). See [Logging and acquisition](../logging.md).

## Public surface

::: watlowlib.sinks

## Base Protocol + `pipe()`

::: watlowlib.sinks.base

## In-memory sink

::: watlowlib.sinks.memory

## CSV sink

::: watlowlib.sinks.csv

## JSONL sink

::: watlowlib.sinks.jsonl

## SQLite sink

::: watlowlib.sinks.sqlite

## Parquet sink (`watlowlib[parquet]`)

::: watlowlib.sinks.parquet

## Postgres sink (`watlowlib[postgres]`)

::: watlowlib.sinks.postgres
