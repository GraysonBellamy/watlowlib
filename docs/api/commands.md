---
description: watlowlib.commands — the Command[Req, Resp] descriptor, per-protocol variants, parameter read/write workhorses, and per-loop helpers.
---

# `watlowlib.commands`

The `Command[Req, Resp]` descriptor, per-protocol variants, the
parameter read/write workhorse pair, and per-loop helpers. See
[Commands](../commands.md) for the catalogue and [Design](../design.md)
§5.

## Public surface

::: watlowlib.commands

## Command base + variants

::: watlowlib.commands.base

## Parameter commands

::: watlowlib.commands.parameters

## Loop helpers

::: watlowlib.commands.loop

## Alarm helpers

::: watlowlib.commands.alarms
